source :rubygems

gem 'rake'
gem 'sinatra'
gem 'resque', :require => "resque/server"
gem 'fog'           # maybe we will need it lately
gem 'sinatra-redis'
gem 'foreman'
gem 'thin'

gem 'activerecord'
gem 'sinatra-activerecord'
gem 'sqlite3'



group :development, :test do
  gem 'pry'
end

group :test do
  gem 'rspec'
  gem 'rack-test'
end
